export type LoginFormData = {
    email: string;
    password: string;
}

export type RegisterFormData = {
    email: string;
    name: string;
    surname: string;
    password: string;
    password_confirmation: string;
}